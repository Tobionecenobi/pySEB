---
title: "pySEB Documentation"
subtitle: "Tutorials for the Scattering Equation Builder"
---

pySEB constructs small-angle-scattering expressions for composite structures
assembled from analytic, symbolic, and numerical subunits.

pySEB combines a native C++ scattering engine with Python bindings and symbolic
backends, optional GiNaC support, and native SymPy conversion. Structures are
built by connecting reusable subunits at named or distributed reference points.

## Install pySEB

Create and activate an isolated environment on Linux or macOS:

```bash
python3 -m venv .venv-pyseb
source .venv-pyseb/bin/activate
python -m pip install --upgrade pip
python -m pip install pyseb
```

Confirm the installation:

```bash
python -c "import pyseb; print(type(pyseb.World()).__name__)"
```

The command prints `World`. Windows PowerShell instructions and source builds
are covered by the
[platform build guides](https://github.com/Tobionecenobi/pySEB/wiki/Build-Guides).

## Start learning

::: {.grid}
::: {.g-col-12 .g-col-md-6}
### New to pySEB?

Start with the scattering ideas and pySEB vocabulary, then install the package
and evaluate your first model step by step.

[Start the introduction](tutorials/introduction.qmd){.btn .btn-primary}
:::

::: {.g-col-12 .g-col-md-6}
### Choose by task

Continue with micelles, nested structures, parameter sweeps, or a mixed
PDB–polymer model. The task pages use tested examples and show the
equivalent C++ APIs where relevant.

[Browse tutorials](tutorials/index.qmd){.btn .btn-primary}
:::

::: {.g-col-12 .g-col-md-6}
### Subunits and YAML models

Learn what a subunit is, explore the generated 3D model catalogue, and define
and validate your own portable YAML subunit.

[Explore or create a subunit](tutorials/custom-yaml-workflow.qmd){.btn .btn-primary}
:::
:::
